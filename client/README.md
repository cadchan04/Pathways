# Client (React)

## Tech Stack
- React (Vite)
- React Router
- Axios

## Setup
```bash
cd client
npm install
npm run dev
```
## Update Node Version
```bash
nvm install --lts # Install the latest Long Term Support (LTS) Version
nvm use --lts
node -v # Check version
```
